[package]
name = "keld-interpreter"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
keld-flow = { path = "../keld-flow" }
keld-ir = { path = "../keld-ir" }
keld-lifecycle = { path = "../keld-lifecycle" }
keld-numeric = { path = "../keld-numeric" }
keld-runtime = { path = "../keld-runtime" }
keld-semantics = { path = "../keld-semantics" }
keld-source = { path = "../keld-source" }

[lints]
workspace = true

